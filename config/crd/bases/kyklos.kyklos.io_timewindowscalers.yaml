---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: timewindowscalers.kyklos.kyklos.io
spec:
  group: kyklos.kyklos.io
  names:
    kind: TimeWindowScaler
    listKind: TimeWindowScalerList
    plural: timewindowscalers
    shortNames:
    - tws
    singular: timewindowscaler
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.targetRef.name
      name: Target
      type: string
    - jsonPath: .spec.defaultReplicas
      name: Default
      type: integer
    - jsonPath: .status.effectiveReplicas
      name: Effective
      type: integer
    - jsonPath: .status.currentWindow
      name: Window
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: TimeWindowScaler is the Schema for the timewindowscalers API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of TimeWindowScaler
            properties:
              defaultReplicas:
                default: 1
                description: DefaultReplicas is the replica count when no windows
                  match
                format: int32
                minimum: 0
                type: integer
              gracePeriodSeconds:
                default: 300
                description: GracePeriodSeconds for scale-down operations
                format: int32
                maximum: 3600
                minimum: 0
                type: integer
              holidayConfigMap:
                description: HolidayConfigMap references a ConfigMap with holiday
                  dates
                type: string
              holidayMode:
                default: ignore
                description: HolidayMode determines how holidays affect scaling
                enum:
                - ignore
                - treat-as-closed
                - treat-as-open
                type: string
              pause:
                default: false
                description: Pause disables all scaling operations
                type: boolean
              targetRef:
                description: TargetRef identifies the Deployment to scale
                properties:
                  name:
                    description: Name of the Deployment
                    type: string
                  namespace:
                    description: Namespace of the Deployment (defaults to TWS namespace)
                    type: string
                required:
                - name
                type: object
              timezone:
                description: Timezone for evaluating time windows (IANA timezone)
                example: America/New_York
                pattern: ^[A-Za-z]+(/[A-Za-z_]+)?(/[A-Za-z_]+)?$
                type: string
              windows:
                description: Windows define time-based scaling rules
                items:
                  description: TimeWindow defines a time-based scaling rule
                  properties:
                    days:
                      description: Days when this window is active
                      items:
                        type: string
                      type: array
                    end:
                      description: End time in HH:MM format (24-hour)
                      pattern: ^([0-1]?[0-9]|2[0-3]):[0-5][0-9]$
                      type: string
                    name:
                      description: Name for this window (used in labels)
                      maxLength: 63
                      type: string
                    replicas:
                      description: Replicas to maintain during this window
                      format: int32
                      minimum: 0
                      type: integer
                    start:
                      description: Start time in HH:MM format (24-hour)
                      pattern: ^([0-1]?[0-9]|2[0-3]):[0-5][0-9]$
                      type: string
                  required:
                  - end
                  - replicas
                  - start
                  type: object
                type: array
            required:
            - defaultReplicas
            - targetRef
            - timezone
            type: object
          status:
            description: status defines the observed state of TimeWindowScaler
            properties:
              conditions:
                description: Conditions represent the latest observations of the resource
                  state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              currentWindow:
                description: CurrentWindow indicates the active time window
                type: string
              effectiveReplicas:
                description: EffectiveReplicas is the computed desired replica count
                format: int32
                type: integer
              gracePeriodExpiry:
                description: GracePeriodExpiry indicates when grace period ends
                format: date-time
                type: string
              lastScaleTime:
                description: LastScaleTime is when the last scaling action occurred
                format: date-time
                type: string
              nextBoundary:
                description: NextBoundary is the next time a scaling action might
                  occur
                format: date-time
                type: string
              observedGeneration:
                description: ObservedGeneration tracks the generation of the spec
                format: int64
                type: integer
              targetObservedReplicas:
                description: TargetObservedReplicas is the observed replica count
                  on the target
                format: int32
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
